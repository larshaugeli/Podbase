<Page
    x:Class="Podbase.APP.Views.PodcastPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:Podbase.Model"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Grid
        x:Name="ContentArea"
        Margin="24,0,0,0">
        <Grid
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}" Margin="0,0,25,0">
            <ListView x:Name="PodcastListView" Height="487" Margin="10,112,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="965"/>

            <TextBlock HorizontalAlignment="Left" Margin="357,30,0,0" Text="Podcasts" TextAlignment="Center" FontSize="20" FontWeight="ExtraBold" TextWrapping="Wrap" VerticalAlignment="Top" Height="27" Width="223"/>
            <CommandBar x:Name="Commands" Height="84" Margin="595,112,0,0" VerticalAlignment="Top" HorizontalAlignment="Stretch">
                <AppBarButton x:Name="Add" Icon="Add" Label="Add" Click="Add_OnClick"/>
                <AppBarButton x:Name="Delete" Icon="Delete" Label="Delete" Command="{x:Bind ViewModel.DeleteCommand}" CommandParameter="{Binding ElementName=gvPodcasts, Path=SelectedItem}"/>
                <AppBarButton x:Name="Edit" Icon="Edit" Label="Edit"/>
                <AppBarButton x:Name="Sort" Icon="Sort" Label="Sort"/>
            </CommandBar>

            <GridView x:Name="gvPodcasts" ItemsSource="{x:Bind ViewModel.Podcasts}" RelativePanel.Below="Commands" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" HorizontalAlignment="Stretch" Margin="0,173,410,0" VerticalAlignment="Stretch">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="model:Podcast">
                        <StackPanel HorizontalAlignment="Left" Width="200" Height="100">
                            <TextBlock Text="{x:Bind Creator}" Style="{StaticResource TitleTextBlockStyle}" />
                            <TextBlock Text="{x:Bind Name}" Style="{StaticResource TitleTextBlockStyle}" />
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

        </Grid>
    </Grid>
</Page>
