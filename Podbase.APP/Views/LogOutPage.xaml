<Page
    x:Class="Podbase.APP.Views.LogOutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Podbase.APP.Views"
    xmlns:viewModels="using:Podbase.APP.ViewModels"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <Grid
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}">
        <Grid Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <TextBlock Text="Log out" TextAlignment="Left" Margin="337,10,346,552" FontSize="40" FontWeight="ExtraBold" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <Button Name="LogOutButton" Content="Log out" HorizontalAlignment="Center" Height="42" Margin="0,245,0,0" VerticalAlignment="Top" Width="152" Click="LogOutButton_Click"></Button>
        </Grid>
    </Grid>
</Page>
